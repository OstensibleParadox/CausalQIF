import CausalQIF.InfoTheory.all

/-!
# CausalQIF.InfoTheory

Grouped entry point for the finite information-theory layer. Re-exports the
`CausalQIF.InfoTheory.*` modules (entropy / KL / marginal / mutual information /
conditional / DPI) via the `all` aggregator, so `import CausalQIF.InfoTheory`
resolves to the full group.
-/
